extension App {
	struct Info {
		static let current = Self()
		private init () { }
		
		let name = "TSI App"
		let shortIdentifier = "TSIApp"
		let identifier = "lv.tsi.app"
		let version = "1.0"
	}
}
